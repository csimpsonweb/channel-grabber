{
  "require": {
    "slim/slim": "^3.1",
    "slim/php-view": "^2.0"
  },
  "scripts": {
    "start": " yarn build; cd public && php -S localhost:8081;",
    "export-test": "rm -rf node_modules/*; rm -rf node_modules/.*; rm -rf vendor/*; zip -r channelgrabber-tech-test-frontend.zip * .[^.]*"
  }
}